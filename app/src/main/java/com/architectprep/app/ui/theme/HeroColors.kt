package com.architectprep.app.ui.theme

import androidx.compose.ui.graphics.Color

/** Foreground colors for the always-dark hero cards (Home, Exam guide) — same in both app themes. */
val HeroText = Color(0xFFF5F1E8)
val HeroTextMuted = Color(0xFFA8A190)
